# overflow-demo

Use `overflow-demo` when you want one predictable large Markdown response.

The tool returns:

- the generated Markdown content
- `vulcan.runtime.overflow_type.page`
- the skill-local template hint `demo-page.md`

This makes it useful for testing:

- host pagination
- spill file creation
- template resolution priority
